import { SearchSongsPipe } from './search-songs.pipe';

describe('SearchSongsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSongsPipe();
    expect(pipe).toBeTruthy();
  });
});
